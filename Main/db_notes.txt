CREATE TABLE reports(
	id int not null auto_increment,
	report_summary varchar(60) not null,
	free_date datetime,
	duration varchar(5),
	priority int,
	create_date timestamp default current_timestamp,
	PRIMARY KEY (id)
);

    
    
   